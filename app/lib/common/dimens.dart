import 'package:flutter/cupertino.dart';

const iconAnimationDuration = Duration(milliseconds: 200);
const iconAnimation300Duration = Duration(milliseconds: 300);
const iconAnimationDoubleDuration = Duration(milliseconds: 400);

const screenPadding = EdgeInsets.only(left: 24.0, right: 24.0, top: 24.0, bottom: 24.0);
const loginInputPadding = EdgeInsets.symmetric(horizontal: 24.0);

const corners = 32.0;
final smallImageRadius = 24.0;
const bigImageRadius = 32.0;

const maxDialogWidth = 500.0;

const buttonHeight = 52.0;
